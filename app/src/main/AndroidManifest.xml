<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.arnav.covid_19hackathonapp">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InstructionsAfterHospital"></activity>
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".HospitalMap"
            android:label="@string/title_activity_hospital_map" />

        <meta-data
            android:name="com.android.geo.APIKey"
            android:value="AIzaSyDOQK9H_NC-vwVweOZH6ttuANRC85qOZcM" />
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-2861264451375282~6876106482" />

        <activity android:name=".create.OtherCreate" />
        <activity android:name=".create.ShieldCreate" />
        <activity android:name=".create.FacemaskCreate" />
        <activity android:name=".DonationSitesList" />
        <activity android:name=".RequestCompletion" />
        <activity android:name=".DonationCompletion" />
        <activity android:name=".requests.forms.OtherRequest" />
        <activity android:name=".requests.forms.FirstRespondersRequest" />
        <activity android:name=".requests.forms.HospitalRequest" />
        <activity android:name=".donations.forms.OtherDonation" />
        <activity android:name=".donations.forms.ShieldDonation" />
        <activity android:name=".donations.forms.FacemaskDonation" />
        <activity android:name=".create.OptionsCreate" />
        <activity android:name=".requests.OptionsRequest" />
        <activity android:name=".donations.OptionsDonate" />
        <activity android:name=".Categories" />
        <activity android:name=".auth.SignUp" />
        <activity android:name=".auth.Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>